using Gtk 4.0;
using Adw 1;

template $AudioDeviceRow : Adw.ActionRow {
  title: bind template.device-name;
  activatable-widget: check_button;

  [prefix]
  CheckButton check_button {
    toggled => $on_toggled(template);
  }
}
