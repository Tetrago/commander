using Adw 1;

template $MonitorButton : Adw.ActionRow {
  title: bind template.monitor-name;
  subtitle: bind template.monitor-description;
  subtitle-lines: 1;
  activatable: true;

  [suffix]
  Image icon {
    icon-name: "video-display-symbolic";
  }

  activated => $on_selected(template);
}
